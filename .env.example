# Copy this file to .env and fill in your values
# .env is gitignored — never commit it

DB_USER=postgres
DB_PASSWORD=
DB_NAME=guestbook

GRAFANA_USER=admin
GRAFANA_PASSWORD=

# Optional — restrict CORS to a specific origin (default: http://localhost)
# CORS_ORIGIN=http://localhost
